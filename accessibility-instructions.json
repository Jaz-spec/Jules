[
    {
        "type": "Associate Label with Input",
        "description": "The 'First name' label is not programmatically associated with its corresponding input field. Add the 'for' attribute to the label and ensure the input has a matching 'id'. This allows screen readers to announce the label when the input is focused and enables users to click the label to focus the input.",
        "prompt": "Correct the 'First name' label in the HTML to be programmatically associated with its input field using the 'for' attribute."
    },
    {
        "type": "Replace Placeholder with Label",
        "description": "The 'Last name' input field uses a placeholder instead of a proper label. This is not accessible because placeholder text disappears on input and is not consistently read by screen readers. Replace the placeholder with a dedicated `<label>` element that is correctly associated with the input.",
        "prompt": "Replace the placeholder in the 'Last name' input field with a proper, associated `<label>` element."
    },
    {
        "type": "Add Alt Text to Image",
        "description": "The image is missing an 'alt' attribute. This attribute provides alternative text for screen readers, describing the image to visually impaired users. If the image is purely decorative, use an empty alt attribute (alt='').",
        "prompt": "Add an appropriate 'alt' attribute to the `<img>` tag to describe the image."
    },
    {
        "type": "Use Semantic Button",
        "description": "A `<div>` element is being used as a button. This is not accessible as it is not focusable with a keyboard by default and does not have the correct semantic meaning. Replace the `<div>` with a `<button>` element to ensure it is accessible to all users.",
        "prompt": "Replace the `<div>` element being used as a 'Submit' button with a semantic `<button>` element."
    }
]
